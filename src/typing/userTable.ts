export interface DataType {
    dataIndex: string;
    title: string;
    tags?: string[];
    hidden?: boolean;
}

export interface AreaType {
    dataIndex: string;
    title: string;
    tags?: string[];
    hidden?: boolean;
}

export interface AreaDataType {
    dataIndex: string;
    title: string;
    tags?: string[];
    hidden?: boolean;
    status?: string;
}
